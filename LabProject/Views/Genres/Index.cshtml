@model IEnumerable<LabProject.Models.Genre>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

@*<p>
    <a asp-action="Create">Create New</a>
</p>*@
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GenreName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GenreName)
            </td>
            <td>
                @*<a asp-action="Edit" asp-route-id="@item.GenreId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GenreId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GenreId">Delete</a>*@
                <a asp-action="ConfirmGenre" asp-route-id="@item.GenreId">Confirm</a>
                @*<form asp-action="ConfirmGenre" method="post">*@
                <input type="hidden" name="movieId" value="@ViewBag.MovieId" />
                <input type="hidden" name="genreId" value="@item.GenreId" />
                console.log(@ViewBag.MovieId)
                console.log(@item.GenreId)
                @*<a>Confirm</a>*@

                @*</form>*@
            </td>
        </tr>
}
    </tbody>
</table>
